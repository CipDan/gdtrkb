# Server-only. Never commit real values — see docs/deployment.md.

# Neon Postgres pooled connection string, SELECT-only role.
DATABASE_URL=

# Public URL of the always-on PostGraphile service (Railway).
POSTGRAPHILE_URL=
